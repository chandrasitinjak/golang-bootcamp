package main

import "fmt"

func main() {

	const fullName string = "Dedi chandra"

	/* Error
	fullName = "ucok"
	*/
	fmt.Println(fullName)

	/*

		Go has 3 operator,
		first :arithmatic operator (+, -, /, *, %, ++, --)

		second :Relational operator (==, !=, >, <, >=, <=)

		third: logical operator (!, &&, || )
	*/
}
